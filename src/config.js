const config = {
    BASE_URL: process.env.NEXT_PUBLIC_SURAJ_COTTON_BASE_URL, // Ensures it's accessible on the client side
    API_URL: process.env.NEXT_PUBLIC_API_BASE,
  };
  
  export default config;
  